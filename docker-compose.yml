version: '3.8'

services:
  workspace:
    build: .
    container_name: mickey-workspace
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - .:/app
      - /Users:/Users
    environment:
      - DATABASE_DIR=/app/data
      - PORT=8000
    restart: unless-stopped
